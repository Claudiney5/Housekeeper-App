import React from 'react'; // biblioteca global
import ReactDOM from 'react-dom'; // métodos específicos para o DOM
import App from './App';


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

